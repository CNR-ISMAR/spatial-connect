# ── Core dependencies ────────────────────────────────────────────────────────
numpy>=1.23
scipy>=1.9

# ── Optional I/O backends ────────────────────────────────────────────────────
# rasterio: preferred raster backend for standalone / dev use.
#   Not required inside QGIS – the plugin falls back to osgeo.gdal (always
#   bundled with QGIS on every platform, including Windows).
rasterio>=1.3          # optional

# fiona: only needed for RasterUtils.vector_to_raster() when rasterio is used.
#   Inside QGIS the fallback path uses osgeo.ogr instead.
fiona>=1.9             # optional

# ── Dev / test dependencies ──────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0

# ── Optional: QGIS is provided by the QGIS environment; not via pip ──────────
# PyQGIS is part of the QGIS installation and should not be listed here.
